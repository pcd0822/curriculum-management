# 고교학점제 진로학업설계 도우미 — 공공데이터 활용 · AI 활용 상세내역서

> 공공데이터 활용 공모전 제출용 산출물 설명서입니다. 본 서비스가 **어떤 공공데이터를 어떻게 활용**했고, **AI를 어떤 방식으로 결합**했는지를 코드·아키텍처 수준에서 상세히 기술합니다.

| 항목 | 내용 |
|------|------|
| 서비스명 | 고교학점제 진로학업설계 도우미 (curriculum-management) |
| 서비스 형태 | 반응형 웹 애플리케이션 (PC/모바일) |
| 배포 환경 | Netlify (정적 호스팅 + 서버리스 Functions) |
| 배포 URL | _※ 제출 시 기입_ |
| 핵심 사용자 | 고교학점제 도입 학교의 **학생 / 교사 / 관리자** |
| 활용 공공데이터 | 공공데이터포털 odcloud(대학 강의계획서·학과 교수 인터뷰), 커리어넷(진로심리검사·학과정보) OpenAPI |
| AI 활용 | OpenAI GPT-4o 기반 진로 맞춤 과목 추천 및 학생부 설계 보조 |

---

## 1. 서비스 개요

### 1-1. 추진 배경
2025년 전면 시행된 **고교학점제**에서 학생은 진로·적성에 따라 과목을 직접 선택해 졸업 학점을 채워야 합니다. 그러나 현장에서는

- 내가 가고 싶은 **대학 학과가 실제로 무엇을 배우는지** 알기 어렵고,
- 그 진로에 **어떤 고교 과목을 들어야 유리한지** 정보가 흩어져 있으며,
- 학교마다 별도 시스템을 운영할 예산·인프라가 부족합니다.

### 1-2. 해결 방안
본 서비스는 **공공데이터로 진로·학업 정보의 근거를 제공**하고, **AI로 진로–과목 연결을 자동 추천**하여 학생의 과목 선택을 돕습니다. 학교별 Google Sheets를 DB로 사용해 **서버 없이 학교 단위로 무상 운영**할 수 있습니다.

```
[학생의 막연한 진로]
   └─(공공데이터)→ 대학 강의계획서 · 교수 인터뷰 · 진로심리검사로 "근거" 확보
   └─(AI)────────→ 희망 진로 ↔ 고교 개설 과목 매핑 자동 추천
   └─(검증엔진)──→ 졸업 학점·교과 비율·선이수 규칙 실시간 점검 후 제출
```

---

## 2. 활용 공공데이터 상세내역

| # | 데이터셋(서비스) | 제공기관 | 데이터셋/일련번호 | 형식 | 활용 화면 |
|---|------------------|----------|-------------------|------|-----------|
| 1 | 대학 학과 강의계획서 | 공공데이터포털(odcloud) | `15112124` (uddi:e4548eca-31d4-4e26-8fda-abe24d9af847) | OpenAPI(JSON) | 진로탐색 → 학과 커리큘럼 |
| 2 | 학과(전공) 교수 인터뷰 | 공공데이터포털(odcloud) | `15090642` (uddi:48606f80-3988-45b4-b532-c71186741e27) | OpenAPI(JSON) | 진로탐색 → 교수 인터뷰 |
| 3 | 진로심리검사(직업흥미검사 등) | 커리어넷(교육부·한국직업능력연구원) | `career.go.kr/inspct/openapi` (v1/v2) | OpenAPI(JSON) | 진로탐색 → 진로심리검사 |
| 4 | 학과정보(MAJOR / MAJOR_VIEW) | 커리어넷 | `career.go.kr/cnet/openapi` | OpenAPI(JSON) | 진로탐색 → 학과 정보 |

> 데이터 규모는 조회 시점 기준으로 강의계획서 **약 6.7만여 건(강좌 단위)**, 학과 교수 인터뷰 **약 320건(학과·교수 단위)** 입니다. (공공데이터는 갱신될 수 있으므로 수치는 활용 시점 측정값)

### 2-1. 대학 학과 강의계획서 (공공데이터포털 odcloud)
- **목적**: 학생이 희망하는 대학 학과가 **실제 어떤 과목을 어느 학년·학기에 몇 학점으로 배우는지**를 보여 진로 의사결정의 근거 제공.
- **활용 필드**: `대학교명`, `단과대학명`, `학부 과명`, `학년`, `학기`, `과목명`, `과목구분`, `학점`, `이론시간`, `실습시간`, `학습목표`, `주교재`, `부교재`, `참고자료`, `선행학습자료`, `연도`.
- **활용 방식**(`netlify/functions/careernet-curriculum.js`):
  - **2단계 탐색** — ① 학과명만 입력 → 해당 학과를 개설한 **대학 목록** 반환, ② 학과명 + 대학 선택 → 그 대학의 **커리큘럼** 반환.
  - **정규화**: 한 행=한 강좌를 `{ 학년, 학기, 과목명, 학점, 학습목표, 교재 … }` 구조로 정리.
  - **중복 제거(dedupe)**: 동일 (학년·학기·과목명)에 대해 **가장 최근 연도(연도 필드)** 1건만 유지 → 분반·연도 중복으로 인한 노이즈 제거.
  - **정렬**: 학년 → 학기 → 과목명(가나다) 순.

### 2-2. 학과(전공) 교수 인터뷰 (공공데이터포털 odcloud)
- **목적**: 학과 교수의 **생생한 인터뷰(질문/답변)** 로 전공의 분위기·진로·필요 역량을 전달.
- **활용 필드**: `연번`, `인터뷰제목`(=학과명), `인터뷰이 직업(직위)`, `질문_1~N` / `답변_1~N`(와이드 컬럼).
- **활용 방식**(`netlify/functions/careernet-interview.js`):
  - 와이드 컬럼(`질문_N`/`답변_N`)을 **질문–답변 페어 배열**로 정규화하여 클라이언트가 바로 렌더링 가능하게 변환.
  - 빈 질문은 스킵, 답변 없는 항목은 공백 처리.
  - **개인정보 보호 준수**: 원본에 교수 개인 이름이 없으며, 직위(예: 교수/부교수)만 표기.

### 2-3. 커리어넷 진로심리검사 OpenAPI
- **목적**: 학생의 **흥미·적성 자기이해**를 돕고 과목 선택의 출발점 제공.
- **활용 방식**(`netlify/functions/careernet-test.js`):
  - 검사 문항 조회(v1 `questions`, v2 `tests`/`test`) → 응답 제출 후 결과 리포트(`report`, `report_v2`) 프록시.
  - 직업흥미검사(H형) 등 다수 검사 지원.

### 2-4. 커리어넷 학과정보 OpenAPI
- **목적**: 학과 목록 검색 및 학과 상세 정보 조회.
- **활용 방식**(`netlify/functions/careernet-test.js` — `major_list`, `major_view` 액션): 키워드로 학과를 검색하고 상세 정보를 조회.

> 보조적으로 `careernet-recommendation.js`는 진로 키워드 기반 과목 추천을 제공하며, 커리어넷 추천 API(`CAREERNET_API_*`) 연동 또는 키워드 매핑(fallback)으로 동작합니다.

---

## 3. 공공데이터 처리·연계 방식 (공통 설계)

| 설계 요소 | 내용 | 근거 |
|-----------|------|------|
| **인증키 서버 보관** | 모든 공공데이터 호출은 **Netlify 서버리스 Function에서만** 수행. `ODCLOUD_API_KEY`, `CAREERNET_API_KEY`를 환경변수로 보관하여 **브라우저에 키 미노출**. | `netlify/functions/*` |
| **서버사이드 검색** | odcloud의 `cond[필드명::LIKE]=값` 조건으로 학과명 등을 **서버에서 필터링** → 전송량·클라이언트 부하 최소화. | interview/curriculum 함수 |
| **페이지네이션** | `page`, `perPage`(odcloud 상한 1000, 검증됨) 사용. | curriculum 함수 |
| **정규화·중복제거** | 와이드 컬럼 → 페어 배열, (학년·학기·과목명) 최신 연도 dedupe 등 **2차 가공**으로 활용성 향상. | interview/curriculum 함수 |
| **자동완성(suggest)** | 학과명 입력 시 distinct 후보를 반환해 검색 UX 개선. | interview/curriculum 함수 |
| **CORS·방어 코딩** | CORS 허용, 입력 길이 cap, 비정상 응답 파싱 방어. | 전 함수 공통 |

---

## 4. AI 활용 상세내역

### 4-1. 개요
| 항목 | 내용 |
|------|------|
| 모델 | **OpenAI GPT-4o** |
| 호출 위치 | `netlify/functions/ai-recommendation.js` (서버리스, 키 비노출) |
| 파라미터 | `temperature: 0.3`(일관성 우선), admin 모드 `response_format: json_object`(구조화 출력) |
| 입력 | 희망 진로(텍스트) + 학교 개설 과목 목록(+ 공동교육과정 목록) |

### 4-2. 기능 — 두 가지 모드
**① 학생 모드 (평문 추천)**
- 입력한 **희망 진로**와 학교 **개설 과목 목록**을 바탕으로 가장 유용한 **과목 7개 + 추천 사유 한 문장**을 추천.
- 화면: 수강신청/AI 추천 페이지에서 추천 과목을 선택지에 즉시 반영.

**② 관리자/심화 모드 (JSON 구조화)**
- `balancedRecommendations`(균형 과목 3) · `advancedRecommendations`(공동교육과정 심화 3) · `subjects`(핵심 과목 5~7) · `keywords`(학생부 키워드 5) · `activities`(탐구활동 3) 를 **구조화 JSON**으로 산출.
- 활용: 학생부(세특) 설계·진로 컨설팅 보조 자료.

### 4-3. AI ↔ 공공데이터 결합 가치
- 공공데이터(강의계획서·교수 인터뷰)로 **"무엇을 배우는가"** 를 확인한 뒤, AI가 그 진로를 **"어떤 고교 과목으로 준비하는가"** 로 변환 추천 → **탐색에서 실행(과목 선택)까지** 끊김 없이 연결.

### 4-4. AI 사용 시 안전장치
| 위험 | 대응 |
|------|------|
| 프롬프트 인젝션 | 입력의 제어문자·줄바꿈 제거, 길이 캡(진로 200자/과목목록 1만자/공동교육 200항목) |
| 비용 폭증 | 입력 길이 제한 + `temperature` 고정 |
| 키 노출 | `OPENAI_API_KEY`를 서버리스 환경변수에만 보관, 브라우저 전송 없음 |

---

## 5. 데이터 흐름 아키텍처

```
                        ┌───────────────────────────────────────────┐
[학생/관리자 브라우저] ──┤ Netlify (React SPA + 서버리스 Functions)   │
                        │                                           │
                        │  /.netlify/functions/                     │
   진로탐색 ───────────▶│   ├─ careernet-curriculum.js ─┐           │
   (학과 검색)          │   ├─ careernet-interview.js  ─┼─▶ 공공데이터포털 odcloud
                        │   ├─ careernet-test.js  ──────┼─▶ 커리어넷 OpenAPI
   AI 추천 ────────────▶│   └─ ai-recommendation.js ────┼─▶ OpenAI GPT-4o
                        │      (ODCLOUD/CAREERNET/OPENAI 키는 서버 환경변수)
                        └───────────────────┬───────────────────────┘
                                            │ (수강신청·학적·검증 데이터)
                                  ┌─────────▼──────────┐
                                  │ 학교별 Google Sheets│  ← Apps Script(GAS) DB
                                  └────────────────────┘
```

- 공공데이터/AI 호출은 **모두 서버리스 함수 경유** → API 키는 브라우저에 노출되지 않음.
- 학교 운영 데이터(수강신청·학적·검증)는 **학교별 Google Sheets**에 저장(서버리스·무상).

---

## 6. 활용 화면(기능)별 매핑

| 화면 | 사용 데이터/AI | 학생 체감 효과 |
|------|----------------|----------------|
| 진로탐색 — 학과 커리큘럼 | odcloud 강의계획서 | "이 학과 가면 1학년 때 무엇을 배우나" 확인 |
| 진로탐색 — 교수 인터뷰 | odcloud 교수 인터뷰 | 전공의 분위기·필요 역량을 교수의 말로 이해 |
| 진로탐색 — 진로심리검사 | 커리어넷 검사 API | 흥미·적성 진단으로 진로 출발점 확보 |
| AI 과목 추천 | OpenAI GPT-4o + 학교 개설 과목 | 진로 맞춤 과목·학생부 키워드·탐구활동 제안 |
| 수강신청/이수현황 | (학교 데이터) | 졸업 학점·교과 비율·선이수 규칙 실시간 검증 |

---

## 7. 기대효과 / 활용 가치

- **정보 격차 해소**: 사교육·지역에 의존하던 진로·학과 정보를 **공공데이터로 무상 제공**.
- **근거 기반 과목 선택**: "막연한 진로 희망" → **실제 대학 커리큘럼·교수 인터뷰·심리검사**라는 근거로 전환.
- **교사 업무 경감**: AI가 학생부 키워드·탐구활동 초안을 제시해 진로지도·세특 설계 보조.
- **무상·확산 용이**: 학교별 Google Sheets + 서버리스 구조로 **별도 서버·예산 없이 전국 어느 학교든 도입** 가능.

---

## 8. 개인정보 보호 및 보안

- **학생 인증은 2-factor(학번 + 학생코드)** 만 사용하며, **이름 등 개인정보는 수집·저장·검증하지 않음**.
- 공공데이터(교수 인터뷰)도 **개인 이름이 없는** 직위 기반 데이터만 활용.
- 모든 외부 API 키(`ODCLOUD_API_KEY`, `CAREERNET_API_KEY`, `OPENAI_API_KEY`)는 **서버 환경변수에만 보관**, 클라이언트 비노출.
- 학교 데이터는 학교가 소유한 Google Sheets에 저장 → **데이터 주권을 학교가 보유**.

---

## 9. 기술 스택 / 배포

| 구분 | 기술 |
|------|------|
| 프론트엔드 | React 18, Vite 6, Tailwind CSS 4, React Router |
| 서버리스 | Netlify Functions (Node.js) — 공공데이터·AI 프록시 |
| 데이터 저장 | Google Apps Script + Google Sheets (학교별) |
| 외부 연계 | 공공데이터포털 odcloud, 커리어넷 OpenAPI, OpenAI API |

---

## 10. 부록 — 연계 엔드포인트·환경변수

**공공데이터/AI 엔드포인트(서버리스 프록시)**
```
GET  /.netlify/functions/careernet-curriculum?major=간호학과[&university=○○대학교]
GET  /.netlify/functions/careernet-interview?major=언어학
GET  /.netlify/functions/careernet-test?action=questions_v2&q=검사번호
POST /.netlify/functions/careernet-test           # 진로심리검사 결과 리포트
POST /.netlify/functions/ai-recommendation        # GPT-4o 과목 추천
```

**원천 공공데이터 API**
```
odcloud 강의계획서 : https://api.odcloud.kr/api/15112124/v1/uddi:e4548eca-31d4-4e26-8fda-abe24d9af847
odcloud 교수 인터뷰: https://api.odcloud.kr/api/15090642/v1/uddi:48606f80-3988-45b4-b532-c71186741e27
커리어넷 검사     : https://www.career.go.kr/inspct/openapi/(test|v2)
커리어넷 학과정보 : https://www.career.go.kr/cnet/openapi/getOpenApi
```

**환경변수**
| 변수 | 용도 |
|------|------|
| `ODCLOUD_API_KEY` | 공공데이터포털 odcloud(강의계획서·교수 인터뷰) 인증키 |
| `CAREERNET_API_KEY` | 커리어넷 진로심리검사·학과정보 인증키 |
| `OPENAI_API_KEY` | OpenAI GPT-4o(과목 추천) |

---

_본 문서는 서비스 소스코드(`netlify/functions/`)와 실제 연계 구현을 근거로 작성되었습니다. 데이터 건수 등 수치는 공공데이터 갱신에 따라 변동될 수 있습니다._
